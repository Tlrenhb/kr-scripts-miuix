<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2026, kr-scripts-miuix contributors -->
<!-- SPDX-License-Identifier: GPL-3.0 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- miuix-blur declares minSdk 33; its effect paths are RuntimeShader/API-33
         only and the app never invokes them on older systems. -->
    <uses-sdk
        android:minSdkVersion="24"
        tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".KrApplication"
        android:theme="@style/Theme.KrScript"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:configChanges="keyboardHidden|orientation|uiMode|layoutDirection|screenLayout|colorMode|fontScale|smallestScreenSize|density|keyboard|screenSize|touchscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.FloatMonitorService"
            android:exported="false" />

    </application>

</manifest>
