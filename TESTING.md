# 回归测试清单

在已 ROOT 的真机/模拟器上验证（CI 仅保证编译与引擎单测）。

## 引擎

- [ ] 非 ROOT 设备启动不崩溃，脚本以 sh 执行并提示未授权
- [ ] ROOT 设备首页显示「ROOT 已授权」
- [ ] `visible` / `support` 属性随 shell 结果显隐
- [ ] `desc-sh` / `summary-sh` / slice `sh` 动态文本求值正常

## 页面渲染

- [ ] sample.xml 六种节点全部渲染（分组标题、开关、选择器、动作、富文本）
- [ ] 开关切换执行 set 脚本（`$state=1/0`），状态由 get 脚本恢复
- [ ] 选择器显示当前值（get），选择后执行 set 并携带所选值
- [ ] 动作点击弹出日志窗，输出流式刷新，可「停止」中断
- [ ] `confirm="true"` 先弹确认框；`warning` 文案展示
- [ ] 子页面进出返回栈正确；页面菜单（refresh/finish/file/脚本）行为正确
- [ ] 下拉刷新重新解析配置；搜索过滤节点与分组

## 参数弹窗

- [ ] 七种参数类型渲染与取值正确（text/select/multiple/switch/seekbar/color/file）
- [ ] `value-sh` / `options-sh` 求值注入
- [ ] 文件参数打开文件选择器，选回路径回填
- [ ] 确认提交后脚本收到的环境变量与输入一致

## 首页

- [ ] CPU 使用率趋势、内存进度条实时更新
- [ ] 每核频率列表刷新；电池电量/温度展示
- [ ] 电源菜单各项命令执行前有二次确认

## 收藏 & 快捷方式

- [ ] 列表项星标收藏/取消，收藏页聚合展示且可直接交互
- [ ] 「添加到桌面」生成快捷方式，点击直达对应页面

## 悬浮窗

- [ ] 未授权时引导到系统授权页；授权后开启显示悬浮窗
- [ ] 悬浮窗可拖动、点按出现关闭按钮并可关闭
- [ ] 关闭应用进程后悬浮窗消失

## 在线页面

- [ ] WebView 加载在线配置页；外链 page 用浏览器打开
- [ ] JS 桥：网页内 `KrScriptCore.executeShell("echo hi")` 返回 hi
